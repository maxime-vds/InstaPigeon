import Likes from "./Likes"

// make follow and likes work via the same function ?

function postLikesAndFollows() {
  // if (love) {
  //add like to post
  // follows have to be set to accounts (both)

  console.log("click love")
  // fetch and post to db
}

const Cardbar = () => {
  return <Likes click={postLikesAndFollows} />
}

export default Cardbar
